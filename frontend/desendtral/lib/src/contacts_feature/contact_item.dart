/// A placeholder class that represents an entity or model.
class ContactItem {
  const ContactItem(this.id, this.name, this.port, this.e);
  final String name;
  final String port;
  final String e;
  final int id;
}
